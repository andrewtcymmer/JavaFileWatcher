# Compiled class file
**/*.class

# Log file
*.log

# virtual machine crash logs, see http://www.java.com/en/download/help/error_hotspot.xml
**/*/hs_err_pid*

# These IntelliJ IDEA project files are ignored because they are generated by Gradle.
.idea/modules/*
.idea/libraries/*
.idea/gradle.xml
.idea/modules.xml
.idea/tasks.xml
.idea/workspace.xml
out/
lib/

# These Gradle files are ignored because they are re-generated or downloaded by the gradle wrapper, which is checked in.
# Note that .gradle (dot-gradle) is ignored but the gradle dir (without dot) is kept, in order for the wrapper to work.
.gradle/
build/

# Serenity makes this.
target/